# 2026-08-02 桌面提交验收

## 范围

- Google Chrome 与 Microsoft Edge 实际浏览器；
- 1920×1080 与 1440×1024；
- 首页、五段评委演示、预置训练回应、自定义现实情境、第四幕后果对比、最终复盘与退出。

手机适配不在本轮截止前验收范围；实时字幕只验收桌面 Chrome/Edge 的浏览器
Web Speech 路径，港话通服务端流式 ASR 仍不在范围内。

## 结果

- 首页正确显示“3 分钟评委演示”及五模块摘要；
- 五段均可完整前进，也可用“上一段”返回；
- 训练回应与现实情境已预置且可编辑；编辑后必须重新生成/反馈才能继续；
- 自定义样例显示“跨部门伙伴＋柔性跟进”，渠道为低置信度“请确认”；
- 第四幕“承担核对”加载 `DEMO-01`，显示与“先划清责任”的数值和人物反应对比；
- 退出后仍为第一幕，主线数值未改变；
- 演示前后 localStorage 与 sessionStorage 均无新增项目；
- 两个视口均无横向溢出；浏览器控制台 0 error、0 warning。

## 自动验证

- 前端：63 项 Vitest 测试通过；
- ESLint：通过；
- Vite production build：通过；
- 后端：隔离 `.venv` 中 68 项 Pytest 测试通过；
- 路演 PPTX：9 页，逐页渲染检查通过，`slides_test.py` 无溢出。

## 语音稳定性复验

复验时间：2026-08-02，修复上传匿名化、并发槽释放与流式完成稿竞态后。

- 官方 HKGAI Studio Speech 页面确认语音识别和会议转录均为 HTTP 接口；
  唯一公开 WebSocket 是 TTS，因此能力探测继续如实返回
  `upload_supported=true`、`live_supported=false`。
- 新增客户端 `browser-speech` 实时路径：优先请求 `yue-Hant-HK`，浏览器不支持
  时切换 `zh-HK`；它与后端 `live_supported`、港话通文件来源和两个文本模型
  来源完全分离。
- Chrome 与 Edge 在本机均真实暴露 `SpeechRecognition` 和
  `webkitSpeechRecognition`，页面为安全上下文；首次语音输入会先显示浏览器
  厂商云端识别、港话通停录兜底和现实情境脱敏时点说明。
- 自定义情境描述、训练库自由回应和自定义逐轮回应均显示统一的“语音输入、
  上传录音、本机录音”工具；训练库搜索层的语音工具数量为 0。
- 自定义样例在实际浏览器继续识别为“跨部门伙伴＋催进度”，渠道低置信度
  显示“请确认”，且可进入第 1 轮语音回应区。
- 1920×1080 与 1440×1024 的 `body.scrollWidth`、
  `documentElement.scrollWidth` 均等于视口宽度；控制台 0 error、0 warning。
- 组件回归覆盖浏览器 interim/final 顺序、粤语结果无多余空格、语言代码回退、
  完成稿仅采用一次，以及浏览器网络错误后自动调用港话通文件转写。
- 上传组件测试确认 IndexedDB 接收匿名 `Blob` 而不是带原始文件名的 `File`；
  流式 complete 后只采用一次转写，组件卸载会取消延迟文件转写。
- 后端回归确认：没有取得并发槽的被拒请求不会调用 `release`，不会破坏同一
  客户端两个正在执行的语音任务。

## 录制前人工检查

- 使用无敏感信息的 5–10 秒粤语短句分别在录屏所用 Chrome/Edge 试说一次，
  确认当前网络和浏览器账户环境能返回实时字幕；浏览器云端服务可用性与准确率
  不能由无麦克风的自动化会话代替验证。
- 若实时字幕延迟、语言不准或服务不可用，直接停录并等待港话通整段转写；若
  两条语音路径都不可用，使用演示模块内已准备好的可编辑文字，不阻断录制。
