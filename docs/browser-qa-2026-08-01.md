# 自定义情境 V2、评委演示与全训练语音输入浏览器 QA

日期：2026-08-01

语音状态复验：2026-08-02

浏览器：Playwright Chromium、Microsoft Edge、Playwright WebKit；另以用户
Chrome 会话复验真实文件识别。WebKit 是 Safari 同源引擎等价检查，不替代
真实 iPhone/iPad 硬件验收。

本地服务：Vite `127.0.0.1:5173`、FastAPI `127.0.0.1:8000`；训练回合模型设为 mock，验证独立降级路径。

## 视口矩阵

| 视口 | 自定义准备页 | 逐轮训练 | 两轮收口与复盘 | 评委三段导览 | 横向溢出 |
| --- | --- | --- | --- | --- | --- |
| 1440×1024 | 通过 | 通过 | 通过 | 通过 | 无 |
| 390×844 | 通过 | 通过 | 通过 | 通过 | 无 |

## 语音输入矩阵

| 入口 | 键盘 | 麦克风控件 | 上传录音 | 本机录音 | 字数限制 |
| --- | --- | --- | --- | --- | --- |
| 五幕主线 AI 回应 | 通过 | 通过 | 通过 | 通过 | 160 |
| 12 项训练回应 | 通过 | 通过 | 通过 | 通过 | 160 |
| 自定义情境描述 | 通过 | 通过 | 通过 | 通过 | 1000 |
| 自定义逐轮回应 | 通过 | 通过 | 通过 | 通过 | 320 |

训练库搜索框保持文本搜索，没有语音控件。首次上传会先显示“录音与隐私
说明”；拒绝或不支持的文件显示稳定错误，文字输入不受阻断；麦克风权限被
拒绝后隐藏录音启动按钮，上传和键盘仍可用。HKGAI Studio 已公布
Bearer + JSON/Base64 文件识别契约；实时 ASR 仍无 WebSocket 契约，能力
探测如实关闭，未使用 TTS Socket 或整段轮询伪装实时。

## 关键断言

- “跨部门同事一直催我给项目数据，负责人还没有回复……”识别为“跨部门伙伴＋催进度”；无明确渠道时使用即时消息模板，并显示低置信度“请确认”。
- 普通短语“我不知道怎么说明时间和下一步”不再被地址规则误脱敏；真实地址仍会在浏览器发送前清理，并在服务端二次清理。
- 准备页显示“服务端规则＋知识编排”，训练页只把 DeepSeek/港话通描述为回合阶段尝试调用。
- 第二轮后出现“现在结束并复盘”；复盘显示逐轮推进轨迹，复制行动卡成功，提示其不包含原始输入或逐轮回答。
- 评委导览依次覆盖第 1、4、5 幕；第四幕承担核对后加载 `DEMO-01`，显示正反选择的数值与人物反应对比。
- 评委导览开始前和完整退出后 `localStorage` 均为空，底层主线仍为第一幕，状态数值及故事图未变化。
- 两个视口的 `body.scrollWidth` 与 `documentElement.scrollWidth` 均等于视口宽度；浏览器控制台 0 error、0 warning。
- 390×844 下语音工具自动改为两列，逐轮回应、隐私弹层和本机录音入口
  均可达；1440×1024 下主线/训练/自定义输入没有遮挡关键动作。
- 移动端同时显示“进入完整主线”“退出演示”，下一段与底部进入完整五幕主线动作可达。

## 自动化回归

- 前端：55 项 Vitest 测试通过。
- 后端：隔离 `.venv` 中 67 项 Pytest 测试通过。
- ESLint 与 Vite production build 通过。
- 当前 Windows 验收机没有安装 Docker CLI，`docker compose build` 未运行；
  PR CI 已在 Ubuntu runner 成功构建 Web/API 两个独立 Docker 镜像。

## 语音审计补强

补强日期：2026-08-02

- 真实生成并解码 WAV、MP3、MP4/M4A/AAC、原始 AAC、Ogg/Opus 和
  WebM/Opus，全部归一化为 16 kHz 单声道 WAV。
- 流式测试覆盖重复/倒退 sequence、final 合并、PCM 字节时长上限、取消、
  Origin、每客户端并发和启动频率。
- 文件 Adapter 覆盖官方 Bearer + JSON/Base64 请求/响应、认证失败、普通
  4xx、5xx、429、业务错误码和超时的稳定错误映射。
- 真实港话通端到端验收使用无敏感短句“麻烦你确认负责人同更新时间”：
  2.64 秒粤语 WAV 经本地上传端点归一化后成功返回繁体转写，来源
  `hkchat-speech`、警告为空；音频未落盘。
- 官方 Studio Speech 专用脚本只展示 TTS、整文件识别、会议转写三类动作；
  `recognize` / `transcribe` 都在选定完整文件后以 JSON/Base64 提交。专用脚本
  及页面已加载资源中均未发现实时 ASR 或 WebSocket 调用。
- 后端能力测试确认：仅设置任意 WS URL 不会启用生产实时能力；只有显式注入
  已验证 live Adapter 才能开放本地 WebSocket 契约，避免误连 TTS Socket。
- 前端组件测试覆盖麦克风拒绝、开始/停止、录音时锁定文本、ASR 不可用、
  IndexedDB 配额失败后的当前内存 Blob 下载，以及 interim/final 去重、
  追加/替换、超长不截断、失败后从本机录音重试和 iOS M4A MIME 推断。
- 快速停止会关闭仍处于连接中的实时 Socket；录音器初始化失败会立即释放
  麦克风轨道。文件转写覆盖字段时长上限，并验证失败请求仍关闭上传资源。

## Chrome 真实接口复验

复验日期：2026-08-02；浏览器：用户 Chrome 会话；后端使用真实港话通文件
识别，实时 ASR 能力关闭。

- 1440×1024 的自定义情境入口上传一段约 3 秒、无敏感信息的粤语 WAV；隐私
  说明先于文件选择出现，上传后界面显示来源“港话通语音转写”和完整繁体结果
  “麻煩你確認負責人同更新時間？”。
- 同一音频立即出现在“本机录音 (1)”抽屉，提供转写、下载和删除动作；抽屉
  标注“最多 20 条 · 30 天自动清理”。
- 训练库抽屉含 1 个搜索框，抽屉内部语音按钮数为 0；当前训练自由回应区有
  1 个 `UtteranceInput`，显示录音、上传和本机录音三个动作。
- 390×844 下语音工具为两列 Grid，三个动作全部可见；自定义准备页、逐轮训练
  第 1 轮和主线 AI 自由回应均可到达，页面宽度不超过视口。
- 自定义样例识别为“跨部门伙伴＋催进度”，无渠道线索显示“请确认”；准备页
  和训练页均无阻断控件。
- 桌面与手机复验控制台均为 0 error / 0 warning；浏览器 `localStorage` 不含
  测试转写或匿名情境文本。

## Edge 与 WebKit 兼容性复验

复验日期：2026-08-02。

- Microsoft Edge 在 1440×1024 与 390×844 均无横向溢出；`MediaRecorder`
  可用，自定义描述区同时显示“语音输入、上传录音、本机录音”三个动作，手机
  端动作保持两列布局，控制台 0 error / 0 warning。
- Playwright WebKit 在相同两种视口均无横向溢出；该 Windows WebKit 运行时
  不提供 `MediaRecorder`，产品按设计隐藏录音启动动作，同时保留上传、本机
  录音抽屉和键盘输入。
- WebKit 训练库只有一个搜索框，搜索层内没有语音按钮；当前训练回应区仍有
  独立 `UtteranceInput`。自定义描述上传入口先展示隐私说明。
- 使用 1 秒静音 WAV 验证供应商空识别结果：后端返回稳定、可恢复的
  `upstream_unavailable`，界面保留内存录音、重新转写和下载入口，不再把
  JSON `null` 显示为字符串 `None`，已确认文字不会被清空。
- Playwright WebKit for Windows 对 IndexedDB Blob 写入返回 `UnknownError:
  Error preparing Blob/File data to be stored in object store`；产品准确显示“本机
  录音库暂时不可用”并进入内存降级。此为该测试运行时限制，真实 Safari/iOS
  的 IndexedDB Blob 持久化仍需在硬件上完成最终验收。
- 静音 WAV 的预期 502 负向请求会在 WebKit 控制台产生一条资源错误；正常
  页面与 Edge 正向路径无 JavaScript 错误或警告。
